import 'package:flutter/material.dart';

class DoonloadWedgit extends StatelessWidget {
  const DoonloadWedgit({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: CircularProgressIndicator());
  }
}